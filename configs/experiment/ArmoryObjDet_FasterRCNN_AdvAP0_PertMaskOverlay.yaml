# @package _global_

defaults:
  - ArmoryObjDet_FasterRCNN_AdvAP0_PertBgAdditive

  - override /attack@model.modules.input_adv_test: [object_detection_adversary_mask_overlay, mask_pgd]

  # Visualize x_adv in test.
  - override /callbacks@model.modules.input_adv_test.callbacks: [progress_bar, image_visualizer]

  - override /datamodule: [armory_carla_objdet, armory_addon_perturbable_mask_for_carla_objdet]

task_name: "ArmoryObjDet_FasterRCNN_AdvAP0_PertMaskOverlay"
